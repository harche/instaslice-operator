package constants

// DefaultOperatorNamespace is the default namespace where Instaslice components
// are installed when no explicit namespace is provided by the runtime.
const DefaultOperatorNamespace = "openshift-das-operator"
